import { NotBuiltYet } from "@/components/shell/NotBuiltYet";

/**
 * Live trip results route. No route-search API contract exists yet, so this stays the honest
 * "not available" stub: it never renders synthetic routes or source states as if they were real.
 */
export default function TripResultsPage() {
  return <NotBuiltYet title="Results" />;
}
